To run the game on localhost:

- First, run the file 'server.exe'. This will open a terminal to display the log records of the server.

- After running the server, run 'client.exe' to open client window (with GUI) to play the game. Can run 'client.exe' multiple times to open multiple client windows.

Note: Game's instructions is presented in the report file.